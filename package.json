{
  "name": "chapter_4",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
    "start": "npx prisma migrate deploy && npx prisma generate && node ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A dockerized full stack todo application that nodejs/jwt/postgresql/docker",
  "dependencies": {
    "@prisma/client": "^6.4.1",
    "bcryptjs": "^3.0.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.3",
    "prisma": "^6.4.1"
  }
}
